import { Container } from "react-bootstrap";
import Board from "../Board/Board";

const Game = () => {
    return (
        <Container>
            <Board />
        </Container>
    );
};

export default Game;
